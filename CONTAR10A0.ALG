Algoritmo "Contar10a0"

Var

   contador:inteiro

Inicio
      Escreval("Início da Contagem:")
      contador <- 10
      Enquanto (contador >=0) faca
               escreval(contador)
               contador <- contador -1
      FimEnquanto
      Escreva("Final da Contagem!")

Fimalgoritmo